/** @type {import('prettier').Config} */
const config = {
  plugins: ['prettier-plugin-tailwindcss'],
  arrowParens: 'always',
  singleQuote: true,
  tabWidth: 2,
  trailingComma: 'none',
  printWidth: 120,
  semi: true
};

export default config;
