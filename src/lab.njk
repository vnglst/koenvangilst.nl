---
layout: layouts/base.njk
title: Lab
description: A collection of projects, articles, and experiments
permalink: /lab/
---

<div class="max-w-6xl mx-auto px-4 py-8" id="content">
  <h1 class="text-4xl md:text-5xl font-bold mb-8">Lab</h1>

  <div class="prose prose-lg dark:prose-invert max-w-none mb-12">
    <p>A collection of my projects, articles, tutorials, and experiments from over the years.</p>
  </div>

  {# Posts Grid #}
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
    {% for post in collections.posts %}
      <article class="border border-gray-200 dark:border-gray-700 rounded-lg p-6 hover:shadow-lg transition-shadow">
        <h2 class="text-xl font-bold mb-2">
          <a href="{{ post.url }}" class="text-blue-600 dark:text-blue-400 hover:underline">
            {{ post.data.title }}
          </a>
        </h2>

        {% if post.data.publishedAt or post.date %}
        <time datetime="{{ (post.data.publishedAt or post.date) | htmlDateString }}" class="text-sm text-gray-600 dark:text-gray-400">
          {{ (post.data.publishedAt or post.date) | readableDate }}
        </time>
        {% endif %}

        {% if post.data.summary %}
        <p class="mt-3 text-gray-700 dark:text-gray-300">
          {{ post.data.summary }}
        </p>
        {% endif %}

        {% if post.data.tags and post.data.tags.length > 0 %}
        <div class="mt-4 flex flex-wrap gap-2">
          {% for tag in post.data.tags %}
          <span class="px-2 py-1 bg-gray-100 dark:bg-gray-800 text-gray-700 dark:text-gray-300 rounded text-xs">
            {{ tag }}
          </span>
          {% endfor %}
        </div>
        {% endif %}
      </article>
    {% endfor %}
  </div>

  {# Tags List #}
  <div class="mt-12 pt-8 border-t border-gray-200 dark:border-gray-700">
    <h2 class="text-2xl font-bold mb-4">Browse by tag</h2>
    <div class="flex flex-wrap gap-2">
      {% for tag in collections.tagList %}
      <a href="/tag/{{ tag | slugify }}/" class="px-3 py-1 bg-blue-100 dark:bg-blue-900 text-blue-800 dark:text-blue-200 rounded-full text-sm hover:bg-blue-200 dark:hover:bg-blue-800 transition-colors">
        {{ tag }}
      </a>
      {% endfor %}
    </div>
  </div>
</div>
